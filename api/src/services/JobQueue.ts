//import { Queue } from 'bullmq';
//import { Worker } from 'bullmq';

class JobQueue {


    //q = new Queue('Paint');
    //q.add('cars', { color: 'blue' });

    /*worker = new Worker('Paint', async job => {
        if (job.name === 'cars') {
          //await paintCar(job.data.color);
        }
      }); */

}